# 設計書 フェーズ7：X（Twitter）コンテンツ取得

## アーキテクチャ概要

```
Slack メッセージ（X URL 含む）
  ↓
events/route.ts → processSingleMessage()
  ↓
buildEnrichment() [lib/extractor.ts]
  ├─ PDF 添付ファイル処理（既存）
  ├─ 通常 URL コンテンツ取得（既存・X ドメインは除外）
  ├─ 画像添付ファイル処理（既存）
  └─ X URL 処理（新規）
       ↓ extractXUrls() で URL 抽出
       ↓ fetchXTweetContent() で POST リクエスト
       ↓ X_FETCH_ENDPOINT（環境変数）
       ↓ sanitizeForPrompt() でインジェクション対策
       ↓ fullText に追記
  ↓
Notion 保存 + Upstash Vector 保存（既存フロー）
```

---

## 変更ファイル一覧

| ファイル | 変更種別 | 内容 |
|---------|---------|------|
| `lib/extractor.ts` | 更新 | X URL 抽出・ツイート取得・インジェクション対策の関数追加、SKIP_DOMAINS から X を除外 |
| `.env.local` | 更新 | `X_FETCH_ENDPOINT` 追加 |

---

## lib/extractor.ts 変更詳細

### ① SKIP_DOMAINS から X を除外（L34〜L38）

```typescript
// 変更前
const SKIP_DOMAINS = [
  'app.slack.com', 'files.slack.com',
  'twitter.com', 'x.com', 't.co',  // X(Twitter): 認証必須
]

// 変更後
const SKIP_DOMAINS = [
  'app.slack.com', 'files.slack.com',
  // X(Twitter) は fetchXTweetContent() で別途処理
]
```

### ② extractXUrls() 新規追加

```typescript
/**
 * Slack の生テキストから X/Twitter の URL を抽出する
 */
export function extractXUrls(slackRawText: string): string[] {
  const regex = /<(https?:\/\/(?:x\.com|twitter\.com|t\.co)\/[^|>]+)(?:\|[^>]*)?>/g
  const urls: string[] = []
  let match
  while ((match = regex.exec(slackRawText)) !== null) {
    urls.push(match[1])
  }
  return urls
}
```

### ③ sanitizeForPrompt() 新規追加

```typescript
/**
 * 外部取得テキストのプロンプトインジェクション対策
 * - 悪意ある指示パターンを [removed] に置換
 * - 10,000 文字超は先頭 3,000 文字に切り詰め
 */
export function sanitizeForPrompt(text: string): string {
  const INJECTION_PATTERNS = [
    /ignore\s+(all\s+)?previous\s+instructions?/gi,
    /forget\s+(all\s+)?previous\s+instructions?/gi,
    /^system\s*:/gim,
    /^assistant\s*:/gim,
    /^user\s*:/gim,
    /あなたは今から/g,
    /以下の指示に従/g,
    /新しいロールを/g,
  ]

  let sanitized = text
  for (const pattern of INJECTION_PATTERNS) {
    sanitized = sanitized.replace(pattern, '[removed]')
  }

  if (sanitized.length > 10000) {
    sanitized = sanitized.slice(0, 3000)
  }

  return sanitized
}
```

### ④ fetchXTweetContent() 新規追加

```typescript
/**
 * X_FETCH_ENDPOINT に X の投稿 URL を POST して、ツイート内容を返す
 * 取得失敗・非公開・削除済みの場合は空文字を返す
 */
export async function fetchXTweetContent(xUrl: string): Promise<string> {
  const endpoint = process.env.X_FETCH_ENDPOINT
  if (!endpoint) return ''

  try {
    const res = await fetch(endpoint, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ url: xUrl }),
      signal: AbortSignal.timeout(10000),
    })
    if (!res.ok) return ''

    const json = await res.json()
    if (!json.success || !json.tweet) return ''

    const { text, author_screen_name, likes, retweets, created_at, url } = json.tweet

    const raw = [
      `[X投稿: @${author_screen_name}]`,
      text,
      '',
      `👍 いいね: ${likes}  🔁 RT: ${retweets}`,
      `投稿日時: ${created_at}`,
      `URL: ${url}`,
    ].join('\n')

    return sanitizeForPrompt(raw)
  } catch {
    return ''
  }
}
```

### ⑤ buildEnrichment() に X URL 処理を追加

```typescript
export async function buildEnrichment(
  rawSlackText: string,
  files: SlackFile[],
  token: string
): Promise<string> {
  const parts: string[] = []

  // 1. PDF 添付ファイル（既存）
  // ...

  // 2. 通常 URL コンテンツ（既存・X ドメインは extractXUrls で別処理のため除外済み）
  // ...

  // 3. 画像添付ファイル（既存）
  // ...

  // 4. X コンテンツ（新規・最大 2件）
  const xUrls = extractXUrls(rawSlackText)
  for (const url of xUrls.slice(0, 2)) {
    const text = await fetchXTweetContent(url)
    if (text) parts.push(`\n${text}`)
  }

  return parts.join('\n')
}
```

---

## n8n エンドポイント リクエスト仕様

| 項目 | 値 |
|------|---|
| メソッド | POST |
| URL | `process.env.X_FETCH_ENDPOINT` |
| Content-Type | `application/json` |
| ボディ | `{ "url": "<X の投稿 URL>" }` |
| タイムアウト | 10 秒 |

### レスポンス例（成功）

```json
{
  "success": true,
  "tweet": {
    "text": "ツイート本文",
    "author_name": "表示名",
    "author_screen_name": "screen_name",
    "created_at": "Wed Jan 01 00:00:00 +0000 2025",
    "likes": 100,
    "retweets": 50,
    "url": "https://x.com/screen_name/status/123456789",
    "media": []
  }
}
```

### レスポンス例（失敗）

```json
{
  "success": false,
  "error": "Tweet not found or protected"
}
```

---

## Notion 保存フォーマット

X コンテンツは `fullText` に追記される形で既存フローに乗る。Notion ページへの追記イメージ:

```
（Slack メッセージ本文）

[X投稿: @screen_name]
ツイート本文テキスト

👍 いいね: 100  🔁 RT: 50
投稿日時: Wed Jan 01 00:00:00 +0000 2025
URL: https://x.com/screen_name/status/123456789
```

---

## プロンプトインジェクション対策 検知パターン一覧

| パターン | 対象例 |
|---------|--------|
| `ignore (all) previous instructions` | 英語指示文 |
| `forget (all) previous instructions` | 英語指示文 |
| 行頭の `system:` / `assistant:` / `user:` | ロール偽装 |
| `あなたは今から` | 日本語ロール指定 |
| `以下の指示に従` | 日本語指示文 |
| `新しいロールを` | 日本語ロール変更 |

※ 今後パターンは `sanitizeForPrompt()` 内の `INJECTION_PATTERNS` 配列に追加する。

---

## デプロイ前チェックリスト

1. Vercel 環境変数に `X_FETCH_ENDPOINT` を追加する
2. ローカルの `.env.local` に `X_FETCH_ENDPOINT` を追加する
3. n8n ワークフローの Webhook パスが `/webhook/x-fetch-knowledgebot` であることを確認する（Publish 済み）
